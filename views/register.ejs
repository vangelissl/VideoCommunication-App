<!DOCTYPE html>
<html lang="en uk">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Register Form</title>
	<script src="https://cdn.tailwindcss.com"></script>

	<style>
		.custom-blue {
			background-color: #4B79A1;
			transition-duration: 0.5s;
		}

		.custom-blue-hover:hover {
			background-color: #283E51
		}
	</style>
</head>

<body class="bg-gray-100 flex items-center justify-center min-h-screen">

	<div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
		<!-- Navigation Buttons -->
		<div class="flex justify-center space-x-4 mb-6">
			<a href="/auth/login" style="margin-left: 0; margin-right: 0; padding-left: 0; padding-right: 0;">
				<button class="bg-gray-300 text-gray-700 py-2 hover:bg-gray-400 transition w-40"
					style="margin-left: 0; margin-right: 0; padding-left: 0; padding-right: 0;" data-form="login">
					Login
				</button>
			</a>
			<button disabled class="text-white py-2 w-40 custom-blue"
				style="margin-left: 0; margin-right: 0; padding-left: 0; padding-right: 0;" data-form="register">
				Register
			</button>
		</div>

		<!-- Register Form -->
		<form action="/register" method="post">
			<h2 class="text-2xl font-bold text-center text-gray-800 mb-6">Register</h2>
			<div class="space-y-4">
				<div>
					<label for="username" class="block text-sm font-medium text-gray-700">Username</label>
					<input type="name" id="username" name="username"
						class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
						placeholder="username" required value="<%= (user === null) ? "" : user.username%>">
				</div>
				<div>
					<label for="first-name" class="block text-sm font-medium text-gray-700">First name</label>
					<input type="name" id="first-name" name="firstName"
						class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
						placeholder="first name" required value="<%= (user === null) ? "" : user.firstName%>">
				</div>
				<div>
					<label for="last-name" class="block text-sm font-medium text-gray-700">Last name</label>
					<input type="name" id="last-name" name="lastName"
						class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
						placeholder="last name" required value="<%= (user === null) ? "" : user.lastName%>">
				</div>
				<div>
					<label for="register-email" class="block text-sm font-medium text-gray-700">Email</label>
					<input type="email" id="register-email" name="email"
						class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
						placeholder="you@example.com" required value="<%= (user === null) ? "" : user.email%>">
				</div>
				<div>
					<label for="register-password" class="block text-sm font-medium text-gray-700">Password</label>
					<input type="password" id="register-password" name="password"
						class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
						placeholder="password" required value="<%= (user === null) ? "" : user.password%>">
				</div>
				<div>
					<label for="confirm-password" class="block text-sm font-medium text-gray-700">Confirm
						Password</label>
					<input type="password" id="confirm-password" name="confirmedPassword"
						class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
						placeholder="confirm password" required
						value="<%= (user === null) ? "" : user.confirmedPassword%>">
				</div>
				<div class="d-flex justify-content-center error-container">
					<% if (errors) { %>
						<p style="color: red;">
							<%= errors[errors.length-1].msg %>
						</p>
						<% } %>
				</div>
				<button id="registerButton" type="submit"
					class="w-full text-white py-2 rounded custom-blue custom-blue-hover">
					Register
				</button>
				<div class="flex justify-center">
					<a href="/" class="text-black-600 hover:underline">&larr; Back to Home</a>
				</div>

			</div>
		</form>

	</div>
</body>

</html>