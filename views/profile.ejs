<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Profile | Specto</title>
	<!-- Bootstrap CSS -->
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
	<!-- Font Awesome for icons -->
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
	<style>
		.profile-header {
			background: linear-gradient(135deg, #4B79A1, #283E51);
			color: white;
			padding: 3rem 0;
			border-radius: 0 0 20px 20px;
		}

		.profile-img {
			width: 150px;
			height: 150px;
			border-radius: 50%;
			border: 5px solid white;
			object-fit: cover;
		}

		.call-history-item {
			border-radius: 10px;
			transition: transform 0.2s;
		}

		.call-history-item:hover {
			transform: translateY(-2px);
			box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
		}

		.btn-outline-primary {
			border-color: #4B79A1;
			color: #4B79A1;
		}

		.btn-outline-primary:hover {
			background-color: #4B79A1;
			color: white;
		}

		.contact-card {
			border-radius: 10px;
			transition: transform 0.2s;
		}

		.contact-card:hover {
			transform: translateY(-3px);
			box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
		}

		.status-indicator {
			width: 12px;
			height: 12px;
			border-radius: 50%;
			display: inline-block;
			margin-right: 5px;
		}

		.status-online {
			background-color: #28a745;
		}

		.status-offline {
			background-color: #dc3545;
		}

		.status-away {
			background-color: #ffc107;
		}

		.contact-avatar {
			width: 50px;
			height: 50px;
			border-radius: 50%;
			object-fit: cover;
		}

		.navbar-brand {
			font-weight: bold;
			font-size: 1.5rem;
		}

		.profile-avatar {
			width: 150px;
			height: 150px;
			border-radius: 50%;
			border: 5px solid white;
			background: #b8b239;
			color: white;
			font-size: 80px;
			font-weight: 700;
			display: flex;
			justify-content: center;
			align-items: center;
			user-select: none;
			font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
			text-transform: uppercase;
			margin: 0 auto;
		}
        
        .empty-state {
            min-height: 150px;
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
        }
	</style>
</head>

<body>
	<!-- Navigation Bar -->
	<%- include('partials/navbar') %>

		<!-- Profile Header -->
		<section class="profile-header">
			<div class="container text-center">
				<div class="profile-avatar mb-3"><%= user.fullname[0] %></div>
				<h1 class="mb-1">
					<%= user.fullname %>
				</h1>
				<p class="mb-3"><i class="fas fa-envelope me-2"></i>
					<%= user.email %>
				</p>
				<div class="d-flex justify-content-center">
					<button class="btn btn-light me-2"><i class="fas fa-edit me-2"></i>Edit Profile</button>
					<button class="btn btn-primary me-2"><i class="fas fa-video me-2"></i>Start New Call</button>
					<button class="btn btn-success"><i class="fas fa-video me-2"></i>Join</button>
				</div>
			</div>
		</section>

		<!-- Main Content -->
		<div class="container my-5">
			<div class="row justify-content-center">
				<!-- Call History Column - Centered -->
				<div class="col-lg-8 mb-4 mb-lg-0">
					<div class="card shadow-sm">
						<div class="card-header bg-white">
							<div class="d-flex justify-content-between align-items-center">
								<h5 class="mb-0">Recent Calls</h5>
								<a href="#" class="text-decoration-none">See All</a>
							</div>
						</div>
						<div class="card-body">
							<% if (meetings.length > 0) { %>
								<% for (const meeting of meetings) { %>
									<div class="call-history-item p-3 mb-3 border rounded">
										<div class="d-flex justify-content-between align-items-center">
											<div class="d-flex align-items-center">
												<div>
													<h6 class="mb-0">
														<%= meeting.title %>
													</h6>
													<small class="text-muted">
														<%= meeting.participants_num %> participants
													</small>
												</div>
											</div>
											<div class="text-end">
												<div class="text-muted mb-1">
													<%= meeting.start_time %>
												</div>
												<div><span class="badge bg-success">
														<%= meeting.duration %>
													</span></div>
											</div>
										</div>
									</div>
								<% } %>
							<% } else { %> 
								<div class="empty-state">
									<div class="text-muted text-center">Your history of meetings is empty</div>
								</div>
							<% } %>
						</div>
					</div>
				</div>
			</div>
		</div>

		<!-- Bootstrap JS Bundle with Popper -->
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>